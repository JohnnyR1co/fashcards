<p>Start check</p>

<%= form_tag(random_path, method: "get", class: "form") do %>
  <p><%= select_tag :search, options_from_collection_for_select(current_user.decks.all, "id", "name"),
                  prompt: t("pages.select") %></p>
  <p><%= submit_tag t("pages.show") %></p>
<% end %>
