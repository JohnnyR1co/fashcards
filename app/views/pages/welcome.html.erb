<p>Первый в мире удобный менеджер флеш-карточек. Именно так.</p>

<%= form_tag({controller: "cards", action: "random"}, :method => "get", class: "form") do %>
  <% @decks = Deck.all %>
  <p><%= select_tag :search, options_from_collection_for_select(@decks, "id", "name"), 
                  prompt: "select a field" %></p>
  <p><%= submit_tag "Show Random" %></p>
<% end %>
